# מעדכן ברי מים — מדריך למשתמש

אפליקציה לעדכון תוכנת ברי המים של שטראוס דרך Wi-Fi.

המסמך מתאר כל מסך, כל כפתור וכל הודעה: על מה ללחוץ, מה יקרה ומה לעשות כשמשהו
משתבש.

> **איך לקרוא.** שמות של אלמנטים באפליקציה מודגשים ב**מודגש**, טקסטים שמופיעים
> על המסך מופיעים `בגופן קבוע רוחב`. המדריך מתייחס לאפליקציה בעברית.

---

## תוכן העניינים

1. [לפני שמתחילים](#1-לפני-שמתחילים)
2. [אלמנטים משותפים](#2-אלמנטים-משותפים)
3. [מסך הגדרות](#3-מסך-הגדרות)
4. [מסך יומן](#4-מסך-יומן)
5. [שלב 1 — התחברות לבר](#5-שלב-1--התחברות-לבר)
6. [רשימת המכשירים](#6-רשימת-המכשירים)
7. [הורדת גרסאות מהשרת](#7-הורדת-גרסאות-מהשרת)
8. [ארכיון הגרסאות שהוחלפו](#8-ארכיון-הגרסאות-שהוחלפו)
9. [שלב 2 — בחירת רכיב](#9-שלב-2--בחירת-רכיב)
10. [שנמוך גרסה](#10-שנמוך-גרסה)
11. [שלב 3 — צריבה](#11-שלב-3--צריבה)
12. [כל ההודעות ומשמעותן](#12-כל-ההודעות-ומשמעותן)
13. [תרחישים נפוצים](#13-תרחישים-נפוצים)
14. [מה לעשות כאשר](#14-מה-לעשות-כאשר)

---

## 1. לפני שמתחילים

דרושים שלושה דברים:

1. **Wi-Fi מופעל בטלפון.** האפליקציה אינה יכולה להפעיל אותו בעצמה — אם הוא כבוי
   תוצג ההודעה `יש להפעיל Wi-Fi` וייפתח חלון ה-Wi-Fi של המערכת.
2. **הבר במצב AP Mode.** במצב זה הוא מרים נקודת גישה משלו. אופן ההעברה למצב זה
   תלוי בדגם — יש לעיין בהוראות המכשיר.
3. **קבצי התוכנה נמצאים בטלפון** — הורדו מהשרת או שנבחרה תיקייה עם קובצי `bin.`
   באופן ידני.

האפליקציה מובילה שלושה שלבים לפי הסדר. אי אפשר לדלג: כל עוד השלב הקודם לא הושלם,
הכפתורים של השלב הבא נעולים.

> `[צילום מסך: המסך הראשי בפתיחת האפליקציה]`

---

## 2. אלמנטים משותפים

האלמנטים הבאים מופיעים בכל שלושת השלבים.

### פס ההתקדמות

שלושה פסים אופקיים בראש המסך. הפס המודגש הוא השלב הנוכחי.

### כותרת השלב

מתחת לפסים שלוש שורות:

- `שלב 1 מתוך 3` — מספר השלב
- שם השלב: `התחברות לבר`, `יש לבחור רכיב לצריבה`, `צריבת תוכנה`
- שורת הסבר שמשתנה לפי המצב

### כפתור יומן

בפינה העליונה. פותח את יומן כל הפעולות עם חותמות זמן. זמין תמיד, גם במהלך צריבה.

### כפתור הגדרות

לצד **יומן**. פותח את מסך ההגדרות.

---

## 3. מסך הגדרות

נפתח באמצעות הכפתור **הגדרות**.

> `[צילום מסך: חלון ההגדרות]`

### שדות

**תחילית שם** — תחילת שמה של נקודת הגישה של הבר, למשל `Water`. האפליקציה מציגה
את רשימת הרשתות של המערכת מסוננת לפי תחילית זו.

> **חשוב: יש הבחנה בין אותיות גדולות לקטנות.** אם הרשת נקראת `WATER_BAR_12`,
> התחילית `Water` לא תתאים — נדרש `WATER`. יש לבדוק את השם המדויק בהגדרות ה-Wi-Fi
> של הטלפון ולהעתיק אות באות.

**סיסמה** — סיסמת נקודת הגישה של הבר.

**הצגת סיסמה** — תיבת סימון שמציגה את הסיסמה כטקסט גלוי, כדי לוודא שאין שגיאת
הקלדה.

**כתובת IP של הבר** — ברירת המחדל `192.168.4.1`. אין לשנות אלא אם ידוע בוודאות
מדוע.

**כתובת שרת הגרסאות** — כתובת מאגר קובצי התוכנה, למשל
`https://accountname.blob.core.windows.net/firmware`.

כל עוד השדה ריק, הקישור **עדכון מהשרת** במסך הראשון **מוסתר**. לאחר מילוי ושמירה
הוא יופיע.

**אסימון SAS** — מפתח גישה למאגר. יש להשאיר ריק אם הגישה ציבורית.

### תיבות סימון

**transactionComplete=true** — פרמטר פרוטוקול. יש להשאיר מסומן, אלא אם מפתח
התוכנה הנחה אחרת.

**ניסיון חוזר אוטומטי ב-HTTP 500** — ניסיון חוזר כשהבר עסוק. יש להשאיר מסומן:
בלעדיו בר עסוק ייראה ככשל.

### שפה

מתג **English / עברית**. המסך ייטען מחדש כדי להחיל את השפה — זו התנהגות תקינה.

### כפתורים

**שמירה** — לשמור ולסגור.
**ביטול** — לסגור ללא שינוי.

> ההגדרות נשמרות בטלפון ונשארות גם לאחר סגירת האפליקציה. הן נמחקות רק בהסרת
> האפליקציה.

---

## 4. מסך יומן

נפתח באמצעות הכפתור **יומן**.

מציג את כל פעולות האפליקציה עם חותמות זמן:

```
[08:54:51]  Folder scanned: Tamar132
[08:55:31]  Looking at the Wi-Fi the phone is already on...
[08:56:04]  No answer: Software caused connection abort
```

> `[צילום מסך: חלון היומן]`

היומן נשאר באנגלית במכוון: זהו פלט טכני שנועד למהנדסים בעת בירור תקלות.

**ניקוי** — לרוקן את היומן.
**סגירה** — לסגור.

**כדאי לפתוח את היומן בכל פעם שמשהו משתבש.** ההודעה על המסך קצרה, ואילו ביומן
רואים באיזה שלב בדיוק נעצר התהליך.

היומן נמחק בסגירת האפליקציה. אם הוא דרוש לבירור — יש לצלם מסך לפני היציאה.

---

## 5. שלב 1 — התחברות לבר

> `[צילום מסך: השלב הראשון במלואו]`

### כרטיס מקור גרסאות התוכנה

מציג מהיכן נלקחים קובצי התוכנה.

**שורת המצב** — אחת מהאפשרויות:

| טקסט | משמעות |
|---|---|
| `לא נבחרה תיקייה` | לא נבחרה תיקייה, אין קבצים |
| `Tamar132 – HMI, ADDON` | בתיקייה נמצאו HMI ותוספת |
| `הורד מהשרת (tamar) – HMI` | בשימוש הקבצים שהורדו מהשרת |

**בחירת תיקייה ←** — פותח את בורר התיקיות של המערכת. יש לבחור את תיקיית השורש,
זו שבתוכה תיקיות המשנה לפי דגמים.

ההרשאה לתיקייה נשמרת: בהפעלה הבאה אין צורך לבחור מחדש.

**עדכון מהשרת ←** — הורדת גרסאות חדשות. מופיע רק אם הוזנה כתובת שרת ב**הגדרות**.
פירוט ב[סעיף 7](#7-הורדת-גרסאות-מהשרת).

**לחיצה ארוכה על עדכון מהשרת** פותחת את ארכיון הגרסאות שהוחלפו —
[סעיף 8](#8-ארכיון-הגרסאות-שהוחלפו).

### כרטיס לפני החיבור

תזכורת: `יש להעביר את בר המים למצב AP.`

**הוראות ←** פותח את רשימת המכשירים הנתמכים —
[סעיף 6](#6-רשימת-המכשירים).

### כפתור התחברות לבר המים

הכפתור הירוק בתחתית המסך. מפעיל את תהליך ההתחברות.

**מה קורה אחרי הלחיצה:**

1. `מחפש בר מים…` — נבדקת הרשת שאליה הטלפון כבר מחובר. אם זהו הבר, החיבור מוכן
   ללא שאלות.

2. אחרת: `מחפש "Water*" – יש לבחור את הבר בחלון`. ייפתח חלון בחירת הרשת **של
   מערכת ההפעלה** עם הרשתות המתאימות. חלון זה מצויר על ידי אנדרואיד, והאפליקציה
   אינה שולטת בעיצובו.

3. יש לבחור את הבר ולאשר את החיבור.

4. `מזהה מכשיר…` — האפליקציה שואלת את הבר מה סוגו ואילו גרסאות מותקנות בו.

5. `מחובר. יש לבדוק את הקישור לפני הצריבה.` — השלב השני נפתח.

**ההתחברות עשויה להימשך עד 30 שניות.** זה תקין, אין ללחוץ שוב על הכפתור.

---

## 6. רשימת המכשירים

נפתחת בקישור **הוראות ←**.

> `[צילום מסך: רשימת המכשירים]`

כל שורה היא דגם אחד: תמונה, שם, סוג התקנה ושם תיקיית התוכנה.

### כשהבר אינו מחובר

מוצגים **כל** הדגמים. אפשר **ללחוץ** על שורה כדי לבחור דגם ידנית.

לשם מה: כדי להוריד מראש את קובצי התוכנה של דגם מסוים, לפני היציאה לשטח, כשהמכשיר
עדיין לא לידך.

מה קורה בלחיצה:

- בשורה הנבחרת מופיע הסימון `נבחר`
- בכרטיס מקור גרסאות התוכנה מופיע `הדגם נבחר ידנית: Tamar`
- האפליקציה עוברת לעבוד עם תיקיית הדגם הזה
- בחלון מופיע הכפתור **ביטול הבחירה**

### כשהבר מחובר

מוצג **רק הדגם המחובר**, עם הסימון `מחובר`. לא ניתן ללחוץ.

> **כך תוכנן.** הדגם נקבע לפי תשובת הבר עצמו ולכן אינו יכול להיות שגוי. בחירה
> ידנית במצב זה הייתה יוצרת סיכון לצריבת תוכנה של דגם אחר. ברגע שהבר עונה, כל
> בחירה ידנית מתבטלת אוטומטית.

**סגירה** — לסגור.

---

## 7. הורדת גרסאות מהשרת

הקישור **עדכון מהשרת ←** בכרטיס מקור גרסאות התוכנה.

### מה קורה

1. `בודק מול השרת…` — נקראת רשימת הגרסאות הזמינות.
2. לכל רכיב מושווית הגרסה שבשרת לזו שכבר נמצאת בטלפון.
3. מורדת **רק גרסה חדשה יותר**. אם בטלפון יש זהה או חדשה יותר — הקובץ לא נוגעים
   בו.
4. `element-p-hmi-0.03.133_enc.bin – 47%` — ההורדה בעיצומה.
5. הקובץ שהורד נבדק לשלמות.
6. הקובץ הישן **מועבר לארכיון**, ולא נמחק.
7. `הורדו 2 קבצים` או `הכול מעודכן`.

### מה מורד

- **הבר מחובר** — רק הגרסאות של הדגם שלו.
- **הבר אינו מחובר** — הגרסאות של כל הדגמים, כדי למלא את הטלפון לפני יציאה לשטח.

### נדרש חיבור לאינטרנט

כל עוד הטלפון מחובר לנקודת הגישה של הבר, אין דרכה אינטרנט. ההורדה תתבצע דרך חבילת
הגלישה.

**סדר העבודה הנכון:** קודם להוריד את הגרסאות במשרד או בבית, ורק אחר כך לצאת לבר.
אם אין חבילת גלישה והטלפון מחובר לבר, ההורדה לא תעבוד.

---

## 8. ארכיון הגרסאות שהוחלפו

נפתח ב**לחיצה ארוכה** על **עדכון מהשרת ←**.

כאן נשמרות גרסאות שהוחלפו בחדשות. האפליקציה אינה מוחקת אותן — אם הגרסה החדשה
מתגלה כבעייתית, הישנה נשארת זמינה.

> `[צילום מסך: חלון הארכיון]`

**ניקוי הארכיון** — מחיקת כל תוכן הארכיון. הכפתור מופיע רק כשיש בו משהו.

**סגירה** — לסגור.

הארכיון אינו מפריע לעבודה: האפליקציה לעולם אינה מציעה קבצים ממנו לצריבה.

---

## 9. שלב 2 — בחירת רכיב

> `[צילום מסך: השלב השני עם כפתורי הרכיבים]`

מתחת לכותרת מופיע איזה בר מחובר ומה נמצא עבורו, למשל `בר S – HMI, ADDON`.

### כפתור בחר גרסת רכיב

מאפשר להחליף את תיקיית קובצי התוכנה מבלי לחזור אחורה.

### שלושת כפתורי הרכיבים

**HMI** — התוכנה הראשית של הבר.
**ADDON** — תוכנת תוספת ה-FIZZZ.
**RC** — תוכנת השלט.

**הטקסט על הכפתור מסביר מה יקרה:**

| על הכפתור | המשמעות | הכפתור פעיל? |
|---|---|---|
| `HMI – לא נמצא` | אין קובץ בתיקייה | לא |
| `HMI  v0.03.133` | יש קובץ, הגרסה שבבר לא ידועה | כן |
| `HMI  v0.03.132  ✓ מעודכן` | בדיוק אותה גרסה כבר מותקנת | **לא** |
| `HMI  v0.03.132 → v0.03.133` | קיים עדכון | כן |

> ## ⚠️ לחיצה על כפתור רכיב מתחילה את הצריבה מיד
>
> לא תופיע בקשת אישור. בחירת הרכיב היא ההחלטה לצרוב אותו. אי אפשר לעצור באמצע.
>
> יש לקרוא את הטקסט על הכפתור לפני הלחיצה.

כפתור עם הסימון `✓ מעודכן` נעול במכוון: אין טעם לצרוב מחדש את אותה גרסה. אם בכל
זאת נדרש — יש להשתמש בשנמוך גרסה.

### כפתור חזרה

חזרה לשלב הראשון.

---

## 10. שנמוך גרסה

הכפתור האדום **שנמוך גרסה…** בתחתית השלב השני.

מאפשר לבחור כל קובץ `bin.` ידנית, כולל גרסה **ישנה יותר**.

**הכפתור אדום משום שחזרה לגרסה ישנה אינה פעולה שגרתית.**

### סדר הפעולות

1. לחיצה על הכפתור — נפתח בורר הקבצים של המערכת.
2. בחירת קובץ `bin.`.
3. האפליקציה מזהה את הרכיב ואת הגרסה לפי שם הקובץ.
4. מופיע חלון אישור.

### חלון האישור

מציג בדיוק מה עומד להתבצע:

```
element-p-hmi-0.03.130_enc.bin

רכיב: hmi
גרסת הקובץ: 0.03.130
מותקן בבר: 0.03.132

הגרסה ישנה יותר מזו המותקנת – שנמוך גרסה.
```

השורה האחרונה מופיעה רק כשמדובר בשנמוך.

שם הכפתור יהיה **התקנה** או **שנמוך גרסה**, לפי המצב. **ביטול** — לביטול.

### סירובים אפשריים

`הקובץ אינו מסוג bin.` — נבחר קובץ מסוג אחר.

`לא ניתן לזהות את הרכיב מתוך "שם"` — לפי שם הקובץ לא ברור לאן לצרוב אותו.
האפליקציה מסרבת ולא מנחשת: צריבת תוכנה לרכיב הלא נכון גרועה מאי-צריבה.

> **אין לשנות את שמות הקבצים.** הגרסה והרכיב נקראים אך ורק מתוך השם.
> `element-p-hmi-0.03.132_enc.bin` תקין, `hmi_new.bin` — האפליקציה תסרב.

---

## 11. שלב 3 — צריבה

> `[צילום מסך: השלב השלישי במהלך צריבה]`

### מה מוצג

**סיכום** — שם הקובץ, גרסה ורכיב.

**פס התקדמות** והטקסט שמתחתיו:

| טקסט | מה קורה |
|---|---|
| `קורא ומחשב סיכום ביקורת עבור שם` | הקובץ נקרא מהטלפון |
| `שולח 47% מתוך 1848 ק"ב` | ההעברה בעיצומה |
| `נשלחו 1848 ק"ב – ממתין לבר` | הכול נשלח, הבר מעבד |
| `הבר עסוק – ניסיון חוזר בעוד 6 שנ'` | הבר עסוק, ספירה לאחור |

**אזהרה:**
`יש להשאיר את האפליקציה פתוחה ואת הבר מחובר לחשמל במהלך העדכון.`

### ⚠️ אין כאן כפתורים

אין **חזרה** ואין **צריבה** בשלב זה. **אי אפשר לעצור את כתיבת התוכנה** — עצירה
באמצע תשאיר את הבר במצב לא מוגדר.

**אין לנתק את החשמל מהבר. אין למזער את האפליקציה. אין לנעול את המסך.**

### סיום

**HMI:**
```
הקובץ נשלח. הבר כותב אותו (כדקה וחצי) ואז מופעל מחדש.
יש להמתין ולהתחבר שוב.
```
הבר יופעל מחדש ונקודת הגישה שלו תיעלם. זה תקין. יש להמתין כדקה וחצי ולהתחבר שוב
כדי לוודא את הגרסה.

**ADDON:**
```
הקובץ נשלח. ה-HMI מעביר אותו ל-STM32 (1–2 דקות).
אין לנתק חשמל. יש לבדוק 'ver' ב-HC.
```
ה-HMI מעביר את התוכנה לתוספת. אין לגעת בחשמל במשך דקה עד שתיים.

לאחר 4 שניות האפליקציה תחזור מעצמה לשלב השני.

### אם נכשל

`נכשל – יש לפתוח את היומן לפרטים` — הסיבה נמצאת ביומן.
`אין תקשורת עם הבר` — הקשר אבד לפני תחילת ההעברה.
`שגיאה: טקסט` — תקלה טכנית, הפירוט המלא ביומן.

---

## 12. כל ההודעות ומשמעותן

### בהתחברות

| הודעה | סיבה | מה לעשות |
|---|---|---|
| `יש להפעיל Wi-Fi` | ה-Wi-Fi כבוי | להפעיל בהגדרות המערכת |
| `יש לפתוח הגדרות ולהזין תחילית שם` | לא הוזנה תחילית | למלא ב**הגדרות** |
| `מחפש "Water*" – יש לבחור את הבר בחלון` | ממתין לבחירת רשת | לבחור את הבר בחלון המערכת |
| `לא מחובר: AP not found` | אין רשת בשם כזה | הבר לא במצב AP, או שהתחילית שגויה |
| `בוצע חיבור, אך הבר לא ענה בכתובת 192.168.4.1` | החיבור אינו לבר | לבדוק את הכתובת ב**הגדרות** |
| `הקישור אבד – יש לחפש שוב` | הקשר נותק | להתחבר מחדש |

### בעבודה עם קבצים

| הודעה | סיבה |
|---|---|
| `אין עדיין קבצי bin. – יש לבחור תיקייה.` | לא נבחרה תיקייה או שהיא ריקה |
| `לא נמצא` על הכפתור | אין קובץ לרכיב זה |

### בהורדה מהשרת

| הודעה | סיבה | מה לעשות |
|---|---|---|
| `יש להזין את כתובת שרת הגרסאות בהגדרות` | לא הוזנה כתובת | למלא ב**הגדרות** |
| `העדכון נכשל: HTTP 404` | אין רשימת גרסאות בכתובת | לבדוק את הכתובת |
| `העדכון נכשל: HTTP 403` | אין הרשאת גישה | לבדוק את אסימון ה-SAS |
| `העדכון נכשל: sha256 mismatch` | הקובץ הורד פגום | לנסות שוב |
| `הכול מעודכן` | הכול עדכני | דבר |

---

## 13. תרחישים נפוצים

### עדכון בר

1. להוריד גרסאות: **עדכון מהשרת ←** (נדרש אינטרנט).
2. להעביר את הבר למצב AP Mode.
3. **התחברות לבר המים**, ולבחור את הבר בחלון המערכת.
4. להסתכל על הכפתורים: היכן שכתוב `← v גרסה חדשה`, קיים עדכון.
5. ללחוץ על כפתור הרכיב — הצריבה מתחילה מיד.
6. להמתין לסיום מבלי לגעת בחשמל.
7. ב-HMI: להמתין להפעלה מחדש, להתחבר שוב ולוודא את הגרסה.

### הכנה ליציאה לשטח

1. **הוראות ←**, ללחוץ על הדגם הרצוי.
2. **עדכון מהשרת ←** — יורדו הגרסאות של דגם זה.
3. לחלופין: לא לבחור דגם כלל — אז יורדו הגרסאות של כל הדגמים.

### חזרה לגרסה קודמת

1. להתחבר לבר.
2. בשלב השני — **שנמוך גרסה…**
3. לבחור את הקובץ הרצוי. גרסאות ישנות שמורות בארכיון.
4. לקרוא את חלון האישור וללחוץ **שנמוך גרסה**.

### עבודה ללא שרת

1. להעתיק את קובצי ה-`bin.` לטלפון, מסודרים בתיקיות לפי דגמים.
2. **בחירת תיקייה ←**, ולבחור את תיקיית השורש.
3. להמשיך כרגיל.

---

## 14. מה לעשות כאשר

**הבר לא נמצא.** לפתוח את הגדרות ה-Wi-Fi של הטלפון ולבדוק אם רשת הבר מופיעה שם
בכלל. אם לא — הבר אינו במצב AP Mode; יש להפעיל אותו מחדש ולהעבירו שוב. אם כן —
להשוות את השם לתחילית שב**הגדרות**, כולל אותיות גדולות וקטנות.

**התחבר אך `לא ענה`.** ההתחברות בוצעה לרשת אחרת בעלת שם דומה. יש לדייק את
התחילית.

**כפתור הרכיב נעול.** או שאין קובץ (`לא נמצא`), או שאותה גרסה כבר מותקנת
(`מעודכן`). במקרה השני — להשתמש בשנמוך גרסה.

**הצריבה חוזרת שוב ושוב.** הבר עסוק. הניסיון החוזר יבצע חמישה ניסיונות במרווחי 8
שניות. אם לא עזר — להפעיל את הבר מחדש ולהתחיל מהתחלה.

**אחרי צריבת HMI האפליקציה איבדה קשר.** כך אמור לקרות: הבר הופעל מחדש. יש להמתין
כדקה וחצי ולהתחבר שוב.

**ההורדה מהשרת לא עובדת.** לבדוק את חבילת הגלישה: נקודת הגישה של הבר אינה מספקת
אינטרנט. יש להתנתק מהבר ולנסות שוב.

**שגיאה לא מובנת.** לפתוח את ה**יומן**, לאתר את השורה בזמן השגיאה, לצלם מסך
ולהעביר למפתח.

---

## מגבלות

- אי אפשר לעצור צריבה שהתחילה.
- קובצי תוכנה שהאפליקציה הורידה נמחקים יחד עם הסרתה.
- אין לשנות את שמות קובצי התוכנה.
- הדגם נקבע על ידי הבר ואינו ניתן לשינוי ידני כאשר קיים חיבור פעיל.