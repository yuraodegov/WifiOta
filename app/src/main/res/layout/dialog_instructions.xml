<?xml version="1.0" encoding="utf-8"?>
<!--
  AP Mode guide: a header that stays put and a scrolling illustration.

  The guide is one tall image rather than laid-out text. It has to live in
  res/drawable-nodpi/ - in a plain res/drawable/ folder Android would treat it
  as mdpi and scale it up threefold on a modern phone, which for this image
  means roughly 1900x6800 pixels in memory.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_marginStart="12dp"
    android:layout_marginTop="24dp"
    android:layout_marginEnd="12dp"
    android:layout_marginBottom="24dp"
    android:background="@drawable/bg_dialog_card"
    android:orientation="vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:paddingStart="20dp"
        android:paddingTop="12dp"
        android:paddingEnd="12dp"
        android:paddingBottom="12dp">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/ap_guide_title"
            android:textColor="@color/text_primary"
            android:textSize="17sp"
            android:textStyle="bold" />

        <ImageButton
            android:id="@+id/guideClose"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:contentDescription="@string/close"
            android:padding="6dp"
            android:src="@drawable/ic_close"
            app:tint="@color/text_secondary" />
    </LinearLayout>

    <!--
      Height 0dp + weight 1: the ScrollView takes exactly what is left below
      the header. With wrap_content it would ask for the full height of a
      2266px image and run off the bottom of the screen instead of scrolling.
    -->
    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:scrollbars="vertical">

        <!--
          The artwork is drawn on white, so it sits on its own white sheet
          instead of the dark card - on the dark background the thin outlines
          and grey captions would be close to unreadable.
        -->
        <ImageView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:adjustViewBounds="true"
            android:background="#FFFFFF"
            android:contentDescription="@string/ap_guide_title"
            android:scaleType="fitCenter"
            android:src="@drawable/ap_mode_guide" />
    </ScrollView>
</LinearLayout>
